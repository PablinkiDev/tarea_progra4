import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-not-profile',
  imports: [RouterLink],
  templateUrl: './not-profile.html',
})
export class NotProfile {}
